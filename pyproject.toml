[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "tidybot-common"
version = "0.1.0"
description = "Shared utilities for TidyBot services"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["logging_config"]
